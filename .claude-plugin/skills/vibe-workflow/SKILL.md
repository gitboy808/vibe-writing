---
name: vibe-workflow
description: Vibe Writing 统筹入口。当用户用自然语言表达写作意图时触发。极简路由：检查项目状态 → 关键词匹配 → 路由到对应 subagent。
version: 0.9.0
---

# Vibe Workflow - 极简路由入口

**版本**: v0.9.0
**职责**: 极简路由（只做状态检查 + 简单路由）
**核心原则**: 主 agent 零任务执行，所有具体工作由 subagent 完成

---

## 🎯 定位

**Vibe Writing 四阶段工作流**：

| 阶段 | Subagent | 触发关键词 |
|------|----------|-----------|
| 学习 | learning-agent | "我想写关于"、"探索"、"开始"、"生成卡片" |
| 结构 | structure-agent | "结构"、"输出"、"分析下一步"、"深度分析" |
| 写作 | writing-agent | "迭代"、"润色"、"整理"、"调整"、"组织白板" |
| 成稿 | draft-agent | "成稿"、"撰写"、"最终输出" |

---

## 🔧 每次对话必须执行

### 步骤1：检测工作目录

```bash
pwd
```

**提取 `{WORK_DIR}`**：从完整路径提取最后一级目录名（如 `vibe-writing-Tasihi`）

### 步骤2：检查项目状态

**读取 `项目/*/项目信息.md`**（如果存在）：

| 字段 | 用途 |
|------|------|
| 项目名 | 显示当前项目 |
| 当前阶段 | 默认路由目标 |
| 当前正在优化 | 断点续传判断 |

### 步骤3：路由决策

**关键词匹配规则**：

| 用户输入关键词 | 路由到 | 特殊处理 |
|---------------|--------|----------|
| "迭代"、"润色"、"整理"、"调整" | writing-agent | - |
| "结构"、"输出"、"分析下一步" | structure-agent | - |
| "成稿"、"撰写"、"最终输出" | draft-agent | - |
| "生成卡片" | learning-agent | 不等4轮 |
| "继续"、"补充" | 按"当前阶段"路由 | - |
| "我想写关于"、"探索"、"开始" | learning-agent | 新项目 |
| "组织白板"、"卡片关系" | writing-agent | 白板组织 |
| 知识卡片路径 + "优化/输出" | structure-agent | - |
| 输出卡片路径 + 提问/讨论 | writing-agent | 需确认 |

**默认路由**：无匹配时，按"当前阶段"路由

### 步骤4：执行路由

```bash
# 学习阶段
Task(subagent_type='learning-agent')

# 结构阶段
Task(subagent_type='structure-agent')

# 写作阶段
Task(subagent_type='writing-agent')

# 成稿阶段
Task(subagent_type='draft-agent')
```

---

## 👋 欢迎语（仅首次/无项目时）

```
欢迎来到 Vibe Writing！

这里实现真正的**人AI共创**——以你为主体，AI为你的思维伙伴：
- **学习**: 对话探索，激发灵感，总结成知识卡片
- **结构**: 分析价值点，决定讲述逻辑
- **写作**: 迭代优化，你把控内容
- **成稿**: 串联所有内容，形成完整文章

📌 可用命令：
- /structure   分析下一步写什么
- /iterate     进入迭代对话
- /polish      直接润色
- /draft       生成最终成稿
- /generate-card  提前生成卡片

开始吧，你想创作什么？
```

---

## 🔄 断点续传

**触发**: 用户说"继续"或"继续XX项目"

**流程**:
1. 读取 `项目/[项目名]/项目信息.md`
2. 检查"当前正在优化"字段：
   - 有内容 → 提示断点续传 + Task(writing-agent)
   - 为空 → 按"当前阶段"路由
3. 无项目 → 欢迎语 + Task(learning-agent)

---

## 📋 Subagent 路径

```
../agents/learning-agent.md   # 学习Agent
../agents/structure-agent.md  # 结构Agent
../agents/writing-agent.md    # 写作Agent
../agents/draft-agent.md      # 成稿Agent
```

---

## 🚫 核心原则

1. **主 agent 只路由，不执行** - 不读取任何任务文件，不做具体工作
2. **Subagent 完全自治** - 每个 subagent 独立上下文，按需读取
3. **状态集中存储** - 项目信息.md 是唯一真相源
4. **上下文零污染** - 每次 Task 调用后上下文清空
5. **用户输入驱动** - 用户说什么，主 agent 就路由什么

---

**使用方式**: 用户表达意图 → vibe-workflow 路由 → subagent 执行 → 返回主 agent
