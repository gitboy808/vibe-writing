# Vibe Writing - 参考文档

本目录包含 Vibe Writing System 的参考文档。

## 🏗️ v0.7.0 架构更新

**重大变更**：从"共享 references 结构"迁移到"核心规范内联模式"

### 变更原因

**问题**：当用户通过 `/plugin install` 全局安装插件时，`.claude-plugin/references/` 路径引用无法正确解析到用户的工作目录。

**解决**：将核心规范直接内联到对应的 skills 中，消除路径依赖。

### 旧架构（v0.6.x）

```
.claude-plugin/
├── skills/
│   ├── canvas-rules/
│   │   └── SKILL.md
│   ├── format-check/
│   │   └── SKILL.md
│   └── core-specs/
│       └── SKILL.md
└── references/
    ├── dual-link.md        ← 被 canvas-rules 引用
    ├── knowledge-card.md   ← 被 format-check 引用
    ├── output-card.md      ← 被 format-check 引用
    └── checkpoint.md       ← 被 core-specs 引用
```

### 新架构（v0.7.0）

```
.claude-plugin/
├── skills/
│   ├── canvas-rules/
│   │   └── SKILL.md        ← dual-link.md 已内联
│   ├── format-check/
│   │   └── SKILL.md        ← knowledge-card.md + output-card.md 已内联
│   └── core-specs/
│       └── SKILL.md        ← checkpoint.md 已内联
└── references/
    ├── example-project.md  # 保留作为完整示例
    └── README.md           # 本文档
```

### 优点

- ✅ **完全消除路径依赖**：安装后立即可用
- ✅ **支持全局安装**：无论插件安装在哪里都能正常工作
- ✅ **符合官方模式**：每个 skill 是独立的、可自包含的单元
- ✅ **减少文件读取**：一次加载完整内容

### 缺点

- ⚠️ 部分内容重复（但这是官方 independent skills 的标准做法）
- ⚠️ 维护时需要同步更新（但可以通过注释标注同步点）

---

## 📚 现有文档

### 示例项目

**完整示例**：`../../项目/Claude和GPT的区别/`

这是一个完整的 Vibe Writing 工作流示例，展示了：
- 2张知识卡片 → 4个输出卡片节点 → 最终成稿（340行）
- 完整的项目信息.md
- 内容白板.canvas
- 所有阶段的文件结构

---

## 🔗 相关资源

- **Skills**: `../skills/` - 核心规范已内联，每个 skill 独立完整
- **Agents**: `../agents/` - 四个工作流Agent
- **Commands**: `../commands/` - 5个快捷命令

## 📖 使用建议

### 对于用户

1. **直接使用 skills** - 所有核心规范已内联到对应 skills
2. **查看示例项目** 了解 Vibe Writing 的实际效果
3. **阅读 skill 文件** 理解具体工作流程

### 对于开发者

1. **研究示例项目** 理解完整工作流
2. **修改 skill 内容** 需要注意：如果多个 skills 有相同规范，需要同步更新
3. **版本标注** - 所有 skills 已更新到 v0.7.0

## 📊 文档关系（新架构）

```
CLAUDE.md (系统入口)
    ↓
agents/ (四个工作流Agent)
    ↓ 引用
skills/ (核心技能 + 内联规范)
    ↓ 独立完整
无需外部引用
```

---

**v0.8.1 更新日期**: 2025-12-28

